apiVersion: v1
kind: Service
metadata:
  name: ms-login
  labels:
    app: ms-login
spec:
  type: ClusterIP
  selector:
    app: ms-login
  ports:
    - name: http
      port: 8080
      targetPort: 8086
