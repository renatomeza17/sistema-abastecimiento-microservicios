apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-recepcion
  labels:
    app: ms-recepcion
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-recepcion
  template:
    metadata:
      labels:
        app: ms-recepcion
    spec:
      initContainers:
        - name: wait-for-services
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z -w 2 config-server 8888 && nc -z -w 2 eureka-server 8761 && nc -z -w 2 db-recepcion 5432; do
                echo "Esperando a config-server, eureka-server y db-recepcion..."
                sleep 2
              done
      containers:
        - name: ms-recepcion
          image: ms-recepcion
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: ms-recepcion-config
            - secretRef:
                name: ms-recepcion-secret
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Deureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/ -Dspring.config.import=optional:configserver:http://config-server:8888"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-recepcion-secret
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-recepcion-secret
                  key: POSTGRES_PASSWORD
          ports:
            - containerPort: 8085
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            periodSeconds: 15
      restartPolicy: Always
