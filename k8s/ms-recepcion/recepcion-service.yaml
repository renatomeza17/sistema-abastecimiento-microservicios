apiVersion: v1
kind: Service
metadata:
  name: ms-recepcion
  labels:
    app: ms-recepcion
spec:
  type: ClusterIP
  selector:
    app: ms-recepcion
  ports:
    - name: http
      port: 8080
      targetPort: 8085
