apiVersion: v1
kind: Service
metadata:
  name: db-requerimiento
  labels:
    app: db-requerimiento
spec:
  type: ClusterIP
  selector:
    app: db-requerimiento
  ports:
    - name: sql
      port: 26257
      targetPort: 26257
    # - name: http-ui
    #   port: 8080
    #   targetPort: 8080
