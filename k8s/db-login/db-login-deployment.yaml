apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-login
  labels:
    app: db-login
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: db-login
  template:
    metadata:
      labels:
        app: db-login
    spec:
      containers:
        - name: db-login
          image: postgres:16-alpine
          envFrom:
            - secretRef:
                name: db-login-secret
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: db-login-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "$(POSTGRES_USER)"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "$(POSTGRES_USER)"]
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: db-login-storage
          persistentVolumeClaim:
            claimName: db-login-pvc
      restartPolicy: Always
