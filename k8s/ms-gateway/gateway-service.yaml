apiVersion: v1
kind: Service
metadata:
  name: ms-gateway
  labels:
    app: ms-gateway
spec:
  type: NodePort
  selector:
    app: ms-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
