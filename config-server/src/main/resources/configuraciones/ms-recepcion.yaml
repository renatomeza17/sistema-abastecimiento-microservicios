server:
  port: 8085

spring:
  application:
    name: ms-recepcion
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres-recepcion:5432/recepcionDb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  swagger-ui:
    path: /docs

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka
